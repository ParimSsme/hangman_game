import 'package:flutter/material.dart';


class AppColors {
  // Primary colors
  static const Color primary = Colors.deepPurple;
  static const Color secondary = Color(0xFF9FA2D6);

  // Backgrounds
  static const Color background = Color(0xFFF5F5F5);

  // Cards
  static const Color card = Color(0xFFDCDCDC);

  // Text
  static const Color textPrimary = Colors.black87;
  static const Color textSecondary = Colors.black54;
  static const Color textOnPrimary = Colors.white;

  // States
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFFC107);

  // Keyboard keys
  static const Color keyActive = primary;
  static const Color keyDisabled = Colors.grey;

  // Hangman drawings
  static const Color hangmanLine = Colors.black;

  static const Color blue = Colors.blueAccent;
  static const Color black = Colors.black;
  static const Color red = Colors.red;
}
